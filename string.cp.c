#include<stdio.h>
#include<string.h>

int main()
{
char str1[50];
char str2[50];

printf(str1, "tavs teksts: ");
scanf("%s", text);
printf("tavs teksts %s.", )

strcpy(str1, "%s");
strcpy(srt2, str1);

return 0;
}
